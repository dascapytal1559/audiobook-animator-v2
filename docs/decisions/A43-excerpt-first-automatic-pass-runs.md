# A43: Excerpt-first: the automatic pass runs on the editor selection or an explicit CLI range; the whole clip requires an explicit flag and the user's say-so.

**Status.** Standing.

**Decision.** Excerpt-first: the automatic pass runs on the editor selection or an explicit CLI range; the whole clip requires an explicit flag and the user's say-so.

**Why.** Q23: the user spot-checks a couple of excerpts first.

**Cited by.** `packages/domain/src/api.ts`, `packages/editor/src/App.tsx`, `packages/editor/src/Timeline.tsx`, `src/commands/timing.ts`, `src/modules/editor-server/timing.ts`

Recorded in the decision index of [CONTEXT.md](../../CONTEXT.md); earlier rounds and evidence are in [docs/history](../history/).
