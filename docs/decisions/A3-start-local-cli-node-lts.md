# A3: Start with a local CLI, Node LTS, pnpm, and exact dependency/configuration pins.

**Status.** Standing. The local CLI is now one tree, `animator <module> <verb>` (see A57).

**Decision.** Start with a local CLI, Node LTS, pnpm, and exact dependency/configuration pins.

**Why.** Fits the available local media and tools; module operations can also be called by a later runner or interface.

**Cited by.** not cited in code.

Recorded in the decision index of [CONTEXT.md](../../CONTEXT.md); earlier rounds and evidence are in [docs/history](../history/).
